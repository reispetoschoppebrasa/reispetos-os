# REI'SPETOS OS v1.4 — Mapa Profissional do Salão

Atualização com mapa automático de 20 mesas e 4 bistrôs, cards de ocupação, filtros, busca, comandas e atualização automática a cada 15 segundos.

## Publicação
Envie `backend`, `frontend`, `README.md`, `docker-compose.yml` e `render.yaml` para o mesmo repositório GitHub. Render e Vercel farão o deploy automaticamente.
